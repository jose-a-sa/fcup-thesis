sudo: required

dist: trusty

before_install:
- sudo apt-get -qq update && sudo apt-get install -y texlive-fonts-recommended texlive-latex-extra texlive-fonts-extra texlive-latex-recommended texlive-xetex

script:
- xelatex -interaction=nonstopmode -halt-on-error main.tex

install:
- xelatex -interaction=nonstopmode -halt-on-error main.tex

notifications:
  email: false